당신의 역할:
- 이 프로젝트(Inbiomat)의 프론트엔드 + 데이터 구조 통합 담당 개발자입니다.
- “지금 당장 실사용 가능한 웹사이트 완성도”를 목표로 판단하세요.

현재 상태 요약:
- 제품 데이터(JSON) 정상화 완료
- 제품 이미지 갤러리 구현 완료
- 모든 경로 절대 경로로 통일 완료
- localhost / Cloudflare Pages 환경에서 정상 동작 확인

이제 당신이 해야 할 일:
1. 지금 코드베이스를 기준으로,
   “이 상태에서 실제 사용자(고객/영업/바이어)가 사용하기에 부족한 점”을 식별하세요.
2. **구조를 깨지 않는 선에서 반드시 필요한 작업만** 선별하세요.
   (파일 이동 ❌ / 디렉터리 구조 변경 ❌)
3. 그중에서
   - 구현 난이도 대비 효과가 가장 큰 작업 1~3개만 선택하세요.
4. 선택한 작업을 실제 코드로 구현하세요.

구현 규칙:
- 절대 경로 규칙 유지
- 기존 기능(제품, 이미지, i18n, 견적 페이지) 절대 훼손 금지
- 리팩토링은 “필요한 최소 범위”에서만 허용
- 추측 금지, 현재 코드 기준으로만 판단

작업이 끝나면 반드시 아래 형식으로 마무리하세요:

[작업 요약]
- 왜 이 작업을 선택했는지
- 사용자 관점에서 어떤 개선이 되는지

[수정/추가된 파일 목록]

[브라우저 테스트 URL]

[다음에 하면 좋은 작업 제안]
- ❗ 이 항목은 실행하지 말고 제안만 할 것